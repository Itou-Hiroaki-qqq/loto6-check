# Railwayからプロジェクトを削除する方法

## 手順

### 1. Railwayダッシュボードにログイン

1. [Railwayダッシュボード](https://railway.app)にログイン
2. 削除したいプロジェクト（`loto6-check`）を選択

### 2. プロジェクト設定を開く

1. プロジェクトの左サイドバーから **「Settings」** をクリック
2. または、プロジェクト名の横にある **「...」**（三点メニュー）をクリック → **「Settings」** を選択

### 3. プロジェクトを削除

1. **「Settings」** ページの一番下までスクロール
2. **「Danger Zone」** セクションを探す
3. **「Delete Project」** または **「Delete」** ボタンをクリック
4. 確認ダイアログが表示されます：
   - プロジェクト名を入力して確認（`loto6-check` と入力）
   - **「Delete」** または **「Confirm」** ボタンをクリック

### 4. 確認

- プロジェクト一覧から `loto6-check` が消えていることを確認
- 削除後は復元できません（データベースのバックアップがある場合は別途確認）

## 注意事項

⚠️ **削除前に確認すること：**

1. **環境変数のバックアップ**
   - 削除前に環境変数（特に `DATABASE_URL`、`AUTO_UPDATE_API_KEY` など）をメモしておく
   - ただし、`loto6-check` は既にVercelに移行済みなので、Vercelの環境変数は別途確認

2. **データベースの確認**
   - `loto6-check` と `loto6-auto-update` は同じNeonデータベースを使用しているはず
   - プロジェクトを削除しても、Neonデータベース自体は削除されません
   - 念のため、Neonデータベースが別途存在することを確認

3. **GitHubリポジトリとの連携**
   - Railwayプロジェクトを削除しても、GitHubリポジトリは削除されません
   - GitHubリポジトリはそのまま残ります

## 代替方法（プロジェクトを一時停止する場合）

完全に削除せず、一時停止したい場合：

1. プロジェクトの **「Settings」** → **「General」** を開く
2. **「Pause Project」** または **「Suspend」** ボタンをクリック
3. これにより、プロジェクトは停止しますが、データは保持されます
4. 再開したい場合は **「Resume」** をクリック

## 削除後の確認

削除が完了したら：

1. Railwayダッシュボードのプロジェクト一覧を確認
2. `loto6-auto-update` プロジェクトが正常に動作していることを確認
3. cron-job.orgの設定で、正しいRailway URL（`loto6-auto-update` のURL）が設定されていることを確認
